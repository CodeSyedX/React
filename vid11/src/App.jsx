
import MyComponent from "./myComment.jsx"
// update function = a function as an argi=ument to setState() usually
                // ex setYear(y => y+1)
                // allow for safe updates based on the previous state
                // used with multiple state update and asynchronous function 
                //good practice to use updaate function

function App() {
  

  return (
    <><MyComponent/></>
  
  )
}

export default App
